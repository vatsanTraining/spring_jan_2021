spring:
  application:
    name: product-service
      cloud:    config:      uri:      - http://localhost:8888
      

  boot:
    admin:
      client:
        url:
        - http://localhost:9595